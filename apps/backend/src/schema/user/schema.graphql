extend type Query {
  user(id: ID!): User
  users: [User!]!
}

type User {
  id: ID!
  name: String!
  email: String!
  iconImageURL: String
  description: String
  twitterProfileUrl: String
  links: [Link!]
  comments: [Comment!]
}
